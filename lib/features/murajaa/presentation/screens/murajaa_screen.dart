import 'package:flutter/material.dart';

class MurajaaScreen extends StatelessWidget {
  const MurajaaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Muraja\'a')),
      body: const Center(child: Text('Wiederholungs-Modul')),
    );
  }
}
